@model MercadoBonsai.Web.Models.PerfilViewModel
@{
    ViewData["Title"] = "Meu Perfil - Mercado Bonsai";
}

<div class="container-xxl flex-grow-1 container-p-y">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h3 class="fw-bold mb-1" style="color: #4A7C59;"><i class="icon-base ri ri-user-line me-2"></i>Meu Perfil & Gestão do Viveiro</h3>
            <p class="text-muted mb-0">Mantenha seus dados cadastrais, fiscais, endereço de envio e apresentação do viveiro atualizados</p>
        </div>
    </div>

    @if (TempData["Sucesso"] != null)
    {
        <div class="alert alert-success alert-dismissible mb-4" role="alert">
            <i class="icon-base ri ri-checkbox-circle-line me-2 fs-5"></i> @TempData["Sucesso"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <!-- Card Especial: Cartão de Visitas Digital & Links Diretos (Plano Prata+) -->
    <div class="card border-0 shadow-sm mb-4">
        <div class="card-header bg-success text-white py-3 d-flex justify-content-between align-items-center">
            <h6 class="fw-bold mb-0 text-white"><i class="icon-base ri ri-qr-code-line me-2"></i>Cartão de Visitas Digital & Links Diretos (Plano Prata & Ouro)</h6>
            <span class="badge bg-white text-success fw-bold">Plano Atual: @Model.NomePlano</span>
        </div>
        <div class="card-body p-4">
            @if (Model.LiberarCartaoVisitas)
            {
                <p class="text-muted small mb-3">
                    Seus links diretos são gerados com criptografia para compartilhar sua vitrine e produtos nas redes sociais com máxima segurança:
                </p>
                <div class="row g-3">
                    <div class="col-md-6 col-lg-3">
                        <div class="p-3 bg-light rounded border h-100 d-flex flex-column justify-content-between">
                            <div>
                                <h6 class="fw-bold text-success mb-1"><i class="icon-base ri ri-store-2-line me-1"></i> Minha Vitrine</h6>
                                <small class="text-muted d-block mb-3">Todos os seus bonsais ativos no portal</small>
                            </div>
                            <button type="button" class="btn btn-outline-success btn-sm w-100" onclick="copiarLink('@Model.LinkVitrineCartao', 'Vitrine Geral')">
                                <i class="icon-base ri ri-file-copy-line me-1"></i> Copiar Link Vitrine
                            </button>
                        </div>
                    </div>
                    <div class="col-md-6 col-lg-3">
                        <div class="p-3 bg-light rounded border h-100 d-flex flex-column justify-content-between">
                            <div>
                                <h6 class="fw-bold text-success mb-1"><i class="icon-base ri ri-tools-line me-1"></i> Meus Insumos</h6>
                                <small class="text-muted d-block mb-3">Ferramentas e adubos do seu estoque</small>
                            </div>
                            <button type="button" class="btn btn-outline-success btn-sm w-100" onclick="copiarLink('@Model.LinkInsumosCartao', 'Seção de Insumos')">
                                <i class="icon-base ri ri-file-copy-line me-1"></i> Copiar Link Insumos
                            </button>
                        </div>
                    </div>
                    <div class="col-md-6 col-lg-3">
                        <div class="p-3 bg-light rounded border h-100 d-flex flex-column justify-content-between">
                            <div>
                                <h6 class="fw-bold text-success mb-1"><i class="icon-base ri ri-cup-line me-1"></i> Meus Vasos</h6>
                                <small class="text-muted d-block mb-3">Vasos e cerâmicas do seu viveiro</small>
                            </div>
                            <button type="button" class="btn btn-outline-success btn-sm w-100" onclick="copiarLink('@Model.LinkVasosCartao', 'Seção de Vasos')">
                                <i class="icon-base ri ri-file-copy-line me-1"></i> Copiar Link Vasos
                            </button>
                        </div>
                    </div>
                    <div class="col-md-6 col-lg-3">
                        <div class="p-3 bg-light rounded border h-100 d-flex flex-column justify-content-between">
                            <div>
                                <h6 class="fw-bold text-success mb-1"><i class="icon-base ri ri-auction-line me-1"></i> Leilões & Rifas</h6>
                                <small class="text-muted d-block mb-3">Suas ações e leilões ativos</small>
                            </div>
                            <button type="button" class="btn btn-outline-success btn-sm w-100" onclick="copiarLink('@Model.LinkEngajamentoCartao', 'Leilões e Ações')">
                                <i class="icon-base ri ri-file-copy-line me-1"></i> Copiar Link Leilões
                            </button>
                        </div>
                    </div>
                </div>
            }
            else
            {
                <div class="d-flex align-items-center">
                    <i class="icon-base ri ri-vip-crown-line fs-2 text-warning me-3"></i>
                    <div>
                        <h6 class="fw-bold text-dark mb-1">Recurso Exclusivo a partir do Plano Prata</h6>
                        <p class="text-muted small mb-0">
                            A geração de Cartão de Visitas Digital com links criptografados e endpoints dedicados é exclusiva dos <strong>Planos Prata e Ouro</strong>. 
                            <a asp-controller="Conta" asp-action="Assinatura" class="fw-bold text-success text-decoration-underline ms-1">Fazer Upgrade do Plano</a>
                        </p>
                    </div>
                </div>
            }
        </div>
    </div>

    <form asp-action="MeuPerfil" method="post" enctype="multipart/form-data">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Id" />

        <div class="row g-4">
            <!-- Coluna Principal (Formulários) -->
            <div class="col-lg-8">

                <!-- REQUIREMENT 3.1: Dados Pessoais e Contato -->
                <div class="card border-0 shadow-sm mb-4">
                    <div class="card-header bg-light py-3">
                        <h6 class="fw-bold mb-0 text-dark"><i class="icon-base ri ri-user-line me-2"></i>1. Dados Pessoais & Contato</h6>
                    </div>
                    <div class="card-body p-4">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-3" role="alert"></div>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="Nome" class="form-label fw-semibold"></label>
                                <input asp-for="Nome" class="form-control" placeholder="Seu nome completo" />
                                <span asp-validation-for="Nome" class="text-danger small"></span>
                            </div>
                            <div class="col-md-6">
                                <label asp-for="Email" class="form-label fw-semibold"></label>
                                <input asp-for="Email" class="form-control" readonly />
                                <small class="text-muted">O e-mail de acesso não pode ser alterado.</small>
                            </div>
                            <div class="col-md-6">
                                <label asp-for="Telefone" class="form-label fw-semibold"></label>
                                <input asp-for="Telefone" id="inputTelefone" class="form-control" placeholder="(00) 00000-0000" />
                                <small class="text-muted">Obrigatório para cálculo de frete por transportadora.</small>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-semibold">Perfil de Acesso</label>
                                <input class="form-control text-capitalize" value="@Model.Perfil.ToString()" readonly />
                            </div>
                        </div>
                    </div>
                </div>

                <!-- REQUIREMENT 3.2: Dados Fiscais -->
                <div class="card border-0 shadow-sm mb-4">
                    <div class="card-header bg-light py-3 d-flex justify-content-between align-items-center">
                        <h6 class="fw-bold mb-0 text-dark"><i class="icon-base ri ri-file-text-line me-2"></i>2. Dados Fiscais (Exigidos para Envio e Emissão)</h6>
                        <span class="badge bg-label-info">Obrigatório para Vendedores</span>
                    </div>
                    <div class="card-body p-4">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="RazaoSocial" class="form-label fw-semibold"></label>
                                <input asp-for="RazaoSocial" class="form-control" placeholder="Nome Fantasia / Razão Social" />
                            </div>
                            <div class="col-md-6">
                                <label asp-for="CpfCnpj" class="form-label fw-semibold"></label>
                                <input asp-for="CpfCnpj" id="inputCpfCnpj" class="form-control" placeholder="CPF ou CNPJ (somente números)" />
                                <small class="text-muted">Obrigatório para o cálculo de frete (Melhor Envio).</small>
                            </div>
                            <div class="col-md-6">
                                <label asp-for="InscricaoEstadual" class="form-label fw-semibold"></label>
                                <input asp-for="InscricaoEstadual" class="form-control" placeholder="I.E. se houver (ou Isento)" />
                            </div>
                        </div>
                    </div>
                </div>

                <!-- REQUIREMENT 3.3: Endereço Completo de Origem do Envio (Melhor Envio) -->
                <div class="card border-0 shadow-sm mb-4" style="border-left: 4px solid #4A7C59 !important;">
                    <div class="card-header bg-light py-3 d-flex justify-content-between align-items-center">
                        <h6 class="fw-bold mb-0 text-dark"><i class="icon-base ri ri-map-pin-user-line me-2 text-success"></i>3. Endereço Completo de Origem do Envio (Cálculo de Frete)</h6>
                        <span class="badge bg-success">Melhor Envio</span>
                    </div>
                    <div class="card-body p-4">
                        <div class="row g-3">
                            <div class="col-md-4">
                                <label asp-for="Cep" class="form-label fw-semibold"></label>
                                <div class="input-group">
                                    <input asp-for="Cep" id="inputCepOrigem" class="form-control" placeholder="00000-000" maxlength="9" onblur="consultarViaCep(this.value)" />
                                    <button type="button" class="btn btn-outline-secondary" onclick="consultarViaCep(document.getElementById('inputCepOrigem').value)">
                                        <i class="icon-base ri ri-search-line"></i>
                                    </button>
                                </div>
                                <small id="lblCepStatus" class="text-muted d-block mt-1">Busca automática via CEP.</small>
                            </div>
                            <div class="col-md-8">
                                <label asp-for="Logradouro" class="form-label fw-semibold"></label>
                                <input asp-for="Logradouro" id="inputLogradouro" class="form-control" placeholder="Rua, Avenida, Alameda..." />
                            </div>
                            <div class="col-md-3">
                                <label asp-for="Numero" class="form-label fw-semibold"></label>
                                <input asp-for="Numero" class="form-control" placeholder="Ex: 123" />
                            </div>
                            <div class="col-md-4">
                                <label asp-for="Complemento" class="form-label fw-semibold"></label>
                                <input asp-for="Complemento" class="form-control" placeholder="Bloco, Apto, Sala..." />
                            </div>
                            <div class="col-md-5">
                                <label asp-for="Bairro" class="form-label fw-semibold"></label>
                                <input asp-for="Bairro" id="inputBairro" class="form-control" placeholder="Bairro" />
                            </div>
                            <div class="col-md-8">
                                <label asp-for="Cidade" class="form-label fw-semibold"></label>
                                <input asp-for="Cidade" id="inputCidade" class="form-control" placeholder="Nome da cidade" />
                            </div>
                            <div class="col-md-4">
                                <label asp-for="Estado" class="form-label fw-semibold"></label>
                                <input asp-for="Estado" id="inputEstado" class="form-control text-uppercase" placeholder="UF (Ex: SP)" maxlength="2" />
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Card 4: Dados Financeiros (Repasses) -->
                <div class="card border-0 shadow-sm mb-4">
                    <div class="card-header bg-light py-3">
                        <h6 class="fw-bold mb-0 text-dark"><i class="icon-base ri ri-bank-card-line me-2"></i>4. Dados Financeiros para Recebimento / Repasses</h6>
                    </div>
                    <div class="card-body p-4">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="ChavePix" class="form-label fw-semibold"></label>
                                <input asp-for="ChavePix" class="form-control" placeholder="E-mail, CPF/CNPJ ou Chave Aleatória" />
                            </div>
                            <div class="col-md-6">
                                <label asp-for="Banco" class="form-label fw-semibold"></label>
                                <input asp-for="Banco" class="form-control" placeholder="Ex: Banco do Brasil (001)" />
                            </div>
                            <div class="col-md-6">
                                <label asp-for="Agencia" class="form-label fw-semibold"></label>
                                <input asp-for="Agencia" class="form-control" placeholder="Ex: 1234-5" />
                            </div>
                            <div class="col-md-6">
                                <label asp-for="Conta" class="form-label fw-semibold"></label>
                                <input asp-for="Conta" class="form-control" placeholder="Ex: 98765-4" />
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Card 5: Apresentação do Viveiro -->
                <div class="card border-0 shadow-sm mb-4">
                    <div class="card-header bg-light py-3">
                        <h6 class="fw-bold mb-0 text-dark"><i class="icon-base ri ri-landscape-line me-2"></i>5. Apresentação do Viveiro</h6>
                    </div>
                    <div class="card-body p-4">
                        <div class="mb-3">
                            <label asp-for="DescricaoViveiro" class="form-label fw-semibold"></label>
                            <textarea asp-for="DescricaoViveiro" class="form-control" rows="4" placeholder="Escreva uma breve apresentação do seu viveiro, espécies cultivadas, tempo de experiência e especialidades..."></textarea>
                            <small class="text-muted">Esta descrição é exibida nos cartões patrocinados da Home para destacar o seu viveiro.</small>
                        </div>
                    </div>
                </div>

                <div class="d-flex gap-3">
                    <button type="submit" class="btn btn-success px-4 btn-lg shadow-sm">
                        <i class="icon-base ri ri-save-line me-1"></i> Salvar Perfil Completo
                    </button>
                    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary btn-lg">Cancelar</a>
                </div>

            </div>

            <!-- Coluna Lateral (Logotipo & Auditoria) -->
            <div class="col-lg-4">
                
                <!-- Card: Logotipo do Viveiro -->
                <div class="card border-0 shadow-sm mb-4 text-center">
                    <div class="card-header bg-light py-3">
                        <h6 class="fw-bold mb-0 text-dark"><i class="icon-base ri ri-image-edit-line me-2"></i>Logotipo do Viveiro</h6>
                    </div>
                    <div class="card-body p-4">
                        <div class="mb-3">
                            @if (!string.IsNullOrEmpty(Model.LogotipoUrl))
                            {
                                <img src="@Model.LogotipoUrl" alt="Logotipo do Viveiro" class="img-fluid rounded shadow-sm mb-3" style="max-height: 180px; object-fit: contain;" />
                            }
                            else
                            {
                                <div class="bg-light rounded p-4 mb-3 d-flex flex-column align-items-center justify-content-center">
                                    <i class="icon-base ri ri-store-3-line text-muted" style="font-size: 3rem;"></i>
                                    <span class="text-muted small mt-2">Sem logotipo cadastrado</span>
                                </div>
                            }
                        </div>
                        <div class="text-start">
                            <label asp-for="LogotipoArquivo" class="form-label fw-semibold"></label>
                            <input asp-for="LogotipoArquivo" type="file" class="form-control" accept="image/*" />
                            <small class="text-muted">Formatos: PNG, JPG, WEBP.</small>
                        </div>
                    </div>
                </div>

                <!-- Card: Auditoria de Alteração -->
                <div class="card border-0 shadow-sm">
                    <div class="card-header bg-light py-3">
                        <h6 class="fw-bold mb-0 text-dark"><i class="icon-base ri ri-history-line me-2"></i>Histórico de Alteração</h6>
                    </div>
                    <div class="card-body p-3">
                        <div class="small">
                            <div class="mb-2">
                                <span class="text-muted">Última Atualização:</span><br />
                                <strong>@(Model.DataUltimaAlteracao.HasValue ? Model.DataUltimaAlteracao.Value.ToString("dd/MM/yyyy HH:mm:ss") : "Nenhuma alteração recente")</strong>
                            </div>
                            <div>
                                <span class="text-muted">Atualizado por:</span><br />
                                <strong>@(string.IsNullOrEmpty(Model.UsuarioAlteracaoNome) ? "Próprio Usuário" : Model.UsuarioAlteracaoNome)</strong>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </form>
</div>

@section Scripts {
    <script src="~/starter-kit/assets/js/masks.js"></script>
    <script>
        function copiarLink(url, secao) {
            if (!url) return;
            navigator.clipboard.writeText(url).then(function() {
                Swal.fire({
                    icon: 'success',
                    title: 'Link Copiado!',
                    text: 'O link criptografado da ' + secao + ' foi copiado para a área de transferência.',
                    timer: 2500,
                    showConfirmButton: false
                });
            }).catch(function(err) {
                prompt("Copie seu link direto:", url);
            });
        }

        async function consultarViaCep(cepInput) {
            if (!cepInput) return;
            const cep = cepInput.replace(/\D/g, '');
            const lblStatus = document.getElementById('lblCepStatus');
            
            if (cep.length !== 8) {
                lblStatus.innerHTML = '<span class="text-danger">CEP inválido (deve ter 8 dígitos).</span>';
                return;
            }

            lblStatus.innerHTML = '<span class="text-info"><i class="spinner-border spinner-border-sm me-1"></i> Buscando endereço via ViaCEP...</span>';

            try {
                const response = await fetch(`https://viacep.com.br/ws/${cep}/json/`);
                const data = await response.json();

                if (data.erro) {
                    lblStatus.innerHTML = '<span class="text-danger">CEP não encontrado base do ViaCEP.</span>';
                    return;
                }

                document.getElementById('inputLogradouro').value = data.logradouro || '';
                document.getElementById('inputBairro').value = data.bairro || '';
                document.getElementById('inputCidade').value = data.localidade || '';
                document.getElementById('inputEstado').value = data.uf || '';
                lblStatus.innerHTML = '<span class="text-success"><i class="icon-base ri ri-check-line me-1"></i> Endereço preenchido automaticamente!</span>';
            } catch (err) {
                lblStatus.innerHTML = '<span class="text-muted">Falha na consulta automática. Preencha manualmente.</span>';
            }
        }
    </script>
}
